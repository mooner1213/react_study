import React from 'react';

const Button = () => {
    return (
        <div className="Button">
            <button type="submit">회원가입 완료하기</button>
        </div>
    );
}

export default Button;
